import { defineStore } from "pinia";
import wyr from "../apis/wyrApi";

export const useWyrStore = defineStore({
  id: "wyr",
  state: () => ({
    // some state
  }),
  actions: {
    // some actions
  },
});
